! El objetivo de esta prueba es demostrar que se modificó lo relacionado a ‘proc’, de forma que se lea single-Declaration ::=
! “proc” Identifier “(“ Formal-Parameter-Sequence “)” “~” Command “end”
! El diseño de la prueba será un programa donde se siga la estructura de la regla de producción.
! Se espera que no ocurra ningún error sintáctico, y que genere un ProcDeclaration.

let
    var z: Integer;
    proc suma(var x: Integer, var y: Integer) ~ z := x+y end
in
    suma(2+6)
end