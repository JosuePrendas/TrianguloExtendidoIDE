! El objetivo de esta prueba es demostrar que se se agregó single-Command ::= “let” Declaration “in” Command “end”
! El diseño de la prueba será un programa donde se siga la estructura de la regla de producción y además la parte de Command sea
! single-Command ‘;’ single-Command.
! Se espera que no ocurra ningún error sintáctico, y que genere el debido xml con presencia de un sequential-Command.

let
    var x: Integer
in
    x := 0;
    x := x+1
end