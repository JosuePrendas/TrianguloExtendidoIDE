! El objetivo de esta prueba es demostrar que se agregó single-Command ::=
! “if” Expression “then” Command (“elsif” Expression “then” Command)*  “else” Command “end”, para el caso donde hay varios ‘elsif’.
! El diseño de la prueba será un programa donde se siga la estructura de la regla de producción con varias repeticiones de ‘elsif’.
! Se espera que no ocurra ningún error sintáctico, y que genere el debido IfCommand con la cantidad de ElsifCommand correspondientes
! a los que fueron escritos en el programa.

if 1=1 then
    puteol()
elsif 1=2 then
    puteol()
elsif 1=3 then
    puteol()
elsif 1=4 then
    puteol()
else
    puteol()
end