! El objetivo de esta prueba es demostrar que se agregó single-Command ::=
! “if” Expression “then” Command (“elsif” Expression “then” Command)*  “else” Command “end”, para el caso donde no hay presencia de ‘elsif’.
! El diseño de la prueba será un programa donde se siga la estructura de la regla de producción sin ninguna repetición de ‘elsif’,
! y además las partes de Command sean single-Command ‘;’ single-Command.
! Se espera que no ocurra ningún error sintáctico, y que genere el debido IfCommand sin ningún ElsifCommand, y con presencia de dos
! SequentialCommand.

if 1=1 then
    let
        var x: Integer
    in
        x := 0
    end;
    x := x + 1
else
    x := x +2;
    let
        var y: Integer
    in
        y := x
    end
end