! El objetivo de esta prueba es demostrar que se agregó single-Command ::= “loop” (“while” | “until”) Expression “do” Command “end”.
! El diseño de la prueba será un programa donde se siga la estructura de la regla de producción en el caso de ‘while’, en la cual
! en Command se anidara otro loop pero en el caso de ‘until’, para evidenciar que ambas se agregaron a la misma estructura de ‘loop’.
! Se espera que no ocurra ningún error sintáctico, y que genere un LoopWhileCommand con un LoopUntilCommand dentro.

loop while true do
    loop until true do
        puteol()
    end
end