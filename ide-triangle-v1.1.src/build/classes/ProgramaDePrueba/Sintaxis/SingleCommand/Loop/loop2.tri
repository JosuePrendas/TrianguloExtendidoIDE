! El objetivo de esta prueba es demostrar que se agregó single-Command ::= “loop” (“while” | “until”) Expression “do” Command “end”.
! El diseño de la prueba será un programa donde se siga la estructura de la regla de producción en el caso de ‘while’,
! pero en vez de usar Expression se usará Command.
! Se espera que ocurra un error sintáctico referente a que se esperaba un Expression en vez de un Command.

loop while nothing do
    puteol()
end